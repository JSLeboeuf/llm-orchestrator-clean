# Configuration example - Copy to .env and fill with your values

# API Configuration
API_KEY=your-secure-api-key-here
DATABASE_URL=sqlite:///app/data/llm_orchestrator.db
PORT=8000

# Google Cloud Configuration
GCP_PROJECT_ID=your-project-id
GCP_REGION=us-central1
SERVICE_NAME=llm-orchestrator

# Optional: Advanced Configuration
MAX_INSTANCES=3
MIN_INSTANCES=0
MEMORY=512Mi
CPU=1

# NEVER commit the actual .env file with real values!